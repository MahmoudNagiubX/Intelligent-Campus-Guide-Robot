# Navigator - Python Dependencies
# Target: Raspberry Pi 5 (aarch64, Python 3.11)
# Locked for production deployment. Do not change without testing on Pi.

# Configuration
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Logging
structlog>=24.0.0
loguru>=0.7.0

# STT: Deepgram Nova-3
deepgram-sdk>=6.1.1,<7.0.0
websockets>=12.0

# LLM / Router: Groq
groq>=0.9.0

# Orchestration: Pipecat
pipecat-ai>=1.0.0,<2.0.0

# TTS: edge-tts
edge-tts>=6.1.0

# Wake Word: openWakeWord
openwakeword>=0.6.0
# Pi 5 note: use onnxruntime, NOT onnxruntime-gpu
onnxruntime>=1.18.0
requests>=2.32.0

# Web scraping (ECU knowledge)
beautifulsoup4>=4.12.0

# VAD: Silero
silero-vad>=5.0
# Pi 5 note: CPU-only torch. Install with:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
torchaudio>=2.2.0

# Audio I/O
pyaudio>=0.2.14
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.26.0

# HTTP
httpx>=0.27.0

# MQTT state publisher (optional — disabled by default, skip on Pi if unused)
paho-mqtt>=2.0.0

# Testing (dev only - not needed on Pi in production)
pytest>=8.0.0
pytest-asyncio>=0.23.0
