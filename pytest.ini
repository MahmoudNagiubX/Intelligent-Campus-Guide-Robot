# Navigator - pytest configuration
[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    hardware: requires microphone, speaker, and live cloud credentials
filterwarnings =
    ignore::DeprecationWarning
